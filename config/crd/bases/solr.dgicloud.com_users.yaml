---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: users.solr.dgicloud.com
spec:
  group: solr.dgicloud.com
  names:
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the users API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of User.
            properties:
              secret:
                description: Reference to a secret containing the username and password
                  for the user.
                properties:
                  password-key:
                    description: Key in secret containing the password. Defaults to
                      "password" for compatibility with kubernetes.io/basic-auth secrets.
                    type: string
                  ref:
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                  username-key:
                    description: Key in secret containing the username. Defaults to
                      "username" for compatibility with kubernetes.io/basic-auth secrets.
                    type: string
                required:
                - ref
                type: object
              solrCloud:
                description: Reference to the Solr Cloud instance in which to manage
                  a user.
                properties:
                  ref:
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - ref
                type: object
            required:
            - secret
            - solrCloud
            type: object
          status:
            description: UserStatus defines the observed state of User.
            properties:
              username:
                type: string
            required:
            - username
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
